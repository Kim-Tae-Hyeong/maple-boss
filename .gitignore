# 비밀번호가 들어있는 로컬 전용 파일 — 커밋되지 않아요.
# Apps Script에 붙여넣을 때는 Code.local.gs 를 쓰고, 저장소에는 Code.gs(자리표시자)만 올라가요.
*.local.gs
*.local.js
*.local.json
.env
.env.*

# OS / 편집기 찌꺼기
Thumbs.db
desktop.ini
.DS_Store
.vscode/
